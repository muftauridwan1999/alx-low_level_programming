#include "main.h"
/**
*int _islower -  function that checks for lower character
*
* return: always 0 if true else return 1
*/
int _islower(int c)
{
if (c > 'a' && c < 'z')
{
return (1);
}
else
{
return (0);
}
}
