#include "main.h"
#include <stdio.h>
void print_array(int *a, int n)
{

	int aux;
	aux = 0;
	n = 0;
	
	while (a[n] != '\0')
	while (aux < n)
	{
	printf("%d, ", a[aux]);
	aux++;
	}

{
	printf("\n");
}
}
